{
  "id": "agenr",
  "name": "agenr",
  "description": "agenr memory plugin for OpenClaw",
  "kind": "memory",
  "contracts": {
    "tools": ["agenr_store", "agenr_recall", "agenr_retire", "agenr_update", "agenr_trace"]
  },
  "uiHints": {
    "dbPath": {
      "label": "Database path",
      "help": "SQLite or libSQL path for the agenr knowledge database. Credentials are loaded from agenr's config.json."
    },
    "configPath": {
      "label": "Config path",
      "help": "Optional path to agenr's config.json. Defaults to config.json next to dbPath."
    }
  },
  "configSchema": {
    "type": "object",
    "additionalProperties": false,
    "required": ["dbPath"],
    "properties": {
      "dbPath": {
        "type": "string",
        "minLength": 1
      },
      "configPath": {
        "type": "string",
        "minLength": 1
      }
    }
  }
}
