{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "pnpm: build:debug",
      "type": "shell",
      "command": "pnpm build:debug",
      "options": {
        "cwd": "${workspaceFolder}"
      },
      "group": {
        "kind": "build",
        "isDefault": true
      },
      "problemMatcher": []
    }
  ]
}
