# Optional override for user profile path
AGENR_USER_PROFILE_PATH=./data/user-profile.json
# Server port (default: 3001)
PORT=3001

# Optional API key for /agp/* routes (Bearer token or X-API-Key)
# AGENR_API_KEY=change-me

# Allowed CORS origins (comma-separated). Unset = allow all (local dev)
# AGENR_CORS_ORIGINS=https://agenr.ai,http://localhost:3000

# Execute policy: open | confirm | strict
# AGENR_EXECUTE_POLICY=confirm

# Strict-mode max execute amount in cents (default: 100)
# AGENR_MAX_EXECUTE_AMOUNT=100

# Allow non-HTTPS adapter URLs for local dev only (1 to enable)
# AGENR_ALLOW_HTTP=1
